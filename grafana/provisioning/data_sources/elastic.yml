apiVersion: 1
datasources:
  - name: Elastic
    type: elasticsearch
    url: ${ELASTIC_URL}
    access: proxy
    basicAuth: true
    basicAuthUser: ${ELASTICS_USER}
    basicAuthPassword: ${ELASTIC_PASSWORD}
    jsonData:
      esVersion: 7
      timeField: "@timestamp"
