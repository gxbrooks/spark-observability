# Centralized variables for Ansible playbooks and roles
# This file is automatically generated from variables.yaml by generate_env.py
# Do not edit manually!

# Registry and image configuration (using local registry on Lab2)
registry_host: "lab2.lan:5000"
spark_image: "{{ registry_host }}/spark"
spark_tag: "{{ spark_version }}"

# Paths and directories

# Elasticsearch settings
elastic_agent_version: "8.15.0"

# Kubernetes-specific settings
k8s_namespace: "spark"
