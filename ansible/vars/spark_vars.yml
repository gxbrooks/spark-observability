# Centralized variables for Ansible playbooks and roles
# This file is automatically generated from variables.yaml by generate_env.py
# Do not edit manually!

# Spark version
spark_version: "3.5.1"

# Registry and image configuration (using standard Apache Spark image)
registry_host: "docker.io"
spark_image: "{{ registry_host }}/apache/spark"
spark_tag: "{{ spark_version }}"

# Paths and directories
spark_events_dir: "/mnt/spark-events"
spark_home: "/opt/spark"

# Kubernetes-specific settings
k8s_namespace: "spark"
