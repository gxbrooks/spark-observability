---
# NFS playbooks: application naming mapped from generated nfs context (vars/contexts/nfs_ansible_vars.yml).
nfs_server: "{{ NFS_SERVER }}"
spark_mount_base: "{{ SPARK_MOUNT_BASE }}"
spark_data_mount: "{{ SPARK_DATA_MOUNT }}"
spark_data_source: "{{ SPARK_DATA_SOURCE }}"
nfs_spark_data_export: "{{ NFS_SPARK_DATA_EXPORT }}"
