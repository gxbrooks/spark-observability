---
# Global stop playbook - shuts down all components in reverse order
# Usage: cd ansible && ansible-playbook -i inventory.yml playbooks/stop.yml

- name: Display shutdown banner
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Display shutdown information
      debug:
        msg: |
          ================================================
          Spark Observability Platform - Shutdown
          ================================================
          
          This playbook will stop components in reverse order:
          1. JupyterHub for Spark Development
          2. Spark on Kubernetes (master, workers, history)
          3. Observability Platform (Lab3)
          4. Kubernetes Cluster
          5. NFS Server (Lab2)
          
          Note: Docker and Elastic Agent will remain running
          
          ================================================

# Step 1: Stop JupyterHub (deferred to Spark 4.0)
# - import_playbook: jupyter/stop_jupyter.yml

# Step 2: Stop Spark
- import_playbook: spark/stop.yml

# Step 3: Stop Observability Platform
- import_playbook: observability/stop.yml

# Step 4: Stop Kubernetes
- import_playbook: k8s/stop.yml

# Step 5: Stop NFS
- import_playbook: nfs/stop.yml

# Step 6: Display completion status
- name: Display shutdown completion status
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Display completion message
      debug:
        msg: |
          ================================================
          Spark Observability Platform - Shutdown Complete
          ================================================
          
          All services have been stopped:
          ✓ JupyterHub stopped
          ✓ Spark components stopped
          ✓ Observability platform stopped
          ✓ Kubernetes cluster stopped
          ✓ NFS server stopped
          
          Services still running:
          - Docker (use docker/stop.yml to stop if needed)
          - Elastic Agent (use elastic-agent/stop.yml to manage)
          
          To restart all services:
            cd ansible && ansible-playbook -i inventory.yml playbooks/start.yml
          
          ================================================

