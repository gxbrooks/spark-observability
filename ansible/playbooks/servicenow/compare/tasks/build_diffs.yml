---
# Diff rows are computed in generate_compare_workbook.py from collected raw data.

- name: Mark diff as deferred to workbook generator
  ansible.builtin.set_fact:
    sn_compare_unit_result: >-
      {{ sn_compare_unit_result | combine({'diff': {'computed_by': 'generate_compare_workbook.py'}}, recursive=True) }}
