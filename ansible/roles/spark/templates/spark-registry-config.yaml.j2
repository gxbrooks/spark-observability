apiVersion: v1
kind: ConfigMap
metadata:
  name: local-registry-config
  namespace: spark
data:
  registries.yaml: |
    mirrors:
      "{{ registry_host }}":
        endpoint:
          - "http://{{ registry_host }}"
