# Spark-defaults.conf - Template for runtime configuration
# This template is processed by Ansible to create a ConfigMap that is mounted into containers
# DO NOT EDIT THIS FILE DIRECTLY
#
# For static build-time variables, use variables.yaml with spark-image context
# For dynamic runtime variables, use variables.yaml with spark-runtime context
#
# Default Spark configurations
spark.master                           spark://spark-master:7077
spark.eventLog.enabled                 true
spark.eventLog.dir                     {{ spark_events_dir }}/{{ ansible_hostname }}
spark.history.fs.logDirectory          {{ spark_events_dir }}
spark.executor.memory                  8g
spark.driver.memory                    4g

# Network configuration for better DNS resolution
spark.driver.host                      ${SPARK_LOCAL_HOSTNAME}
spark.driver.bindAddress               0.0.0.0
spark.network.timeout                  {{ spark_network_timeout | default('600s') }}
spark.rpc.askTimeout                   {{ spark_rpc_ask_timeout | default('60s') }}
spark.rpc.lookupTimeout                {{ spark_rpc_lookup_timeout | default('60s') }}

# DNS resolution settings
spark.driver.port                      7078
spark.blockManager.port                7079

# Heartbeat settings
spark.executor.heartbeatInterval       {{ spark_executor_heartbeat_interval | default('120s') }}

# JVM options
spark.executor.extraJavaOptions        -Xlog:gc*:file=/opt/spark/logs/executor.gc.log:utctime,uptime,level,tags,hostname,pid,tid:filecount=5,filesize=100m 
spark.driver.extraJavaOptions          -Xlog:gc*:file=/opt/spark/logs/executor.gc.log:utctime,uptime,level,tags,hostname,pid,tid:filecount=5,filesize=100m
