apiVersion: v1
kind: ConfigMap
metadata:
  name: local-registry-config
  namespace: spark
data:
  registries.yaml: |
    mirrors:
      "localhost:5000":
        endpoint:
          - "http://localhost:5000"
