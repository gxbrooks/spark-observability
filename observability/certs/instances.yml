instances:
  - name: "es01"
    dns:
      # Service names
      - "es01"
      - "kibana"
      # Short hostnames
      - "GaryPC"
      - "garypc"
      # .lan FQDNs (DNS for home network)
      - "GaryPC.lan"
      # Standard names
      - "localhost"
      # Docker/WSL hostnames
      - "host.docker.internal"
      - "docker-desktop"
    ip:
      # Loopback
      - "127.0.0.1"
      # Primary network IP
      - "192.168.1.115"
  - name: "kibana"
    dns:
      # Service names
      - "kibana"
      - "es01"
      # Short hostnames
      - "GaryPC"
      - "garypc"
      # .lan FQDNs (DNS for home network)
      - "GaryPC.lan"
      # Standard names
      - "localhost"
      # Docker/WSL hostnames
      - "host.docker.internal"
      - "docker-desktop"
    ip:
      # Loopback
      - "127.0.0.1"
      # Primary network IP
      - "192.168.1.115"
