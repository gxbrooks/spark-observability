{
  "description": "Pipeline to clone batch_info out of a spark event stream and store it in batch_active_index for matching and display of open batches",
  "processors": [
    {
      "script": {
        "lang": "painless",
        "source": "ctx = ctx.batch_info; ctx._index = 'batch_active_index';  "
      }
    }
  ]
}